export var host = "http://localhost:5000";

export const initializeRoute = `${host}/initialize`;

export const searchRoute = `${host}/searchedTransactions`;

export const getPieChartData = `${host}/piechart`;

export const getBarChartData = `${host}/barchart`;

export const getStatistics = `${host}/statistics`;

export const getCombinedData = `${host}/combined`
